@using System.ComponentModel
@model FlexTemplate.ViewModels.HomeController.HomeIndexViewModel
<!-- start Main Wrapper -->
<div class="main-wrapper scrollspy-container">
  @foreach(var container in Model.Containers)
  {
    @await Component.InvokeAsync(container.Key, new { template = container.Value})}
  }
</div>

<!-- end Main Wrapper -->